# Operations

Run `validate`, `inspect`, `preflight`, `check`, then `pack`. Pin the SDK and plugin versions, retain inventory hashes, observe lifecycle diagnostics, and roll back by restoring the previously pinned package. Static commands do not execute factories. Preflight runs only typecheck, test, and build with `shell:false`.
