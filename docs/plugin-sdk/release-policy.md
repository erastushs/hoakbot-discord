# Release policy

SDK releases begin as `4.0.0-next.N`. Promotion requires schema identity, export allowlist, generated consumer, example, docs/link/snippet, packaging, and supported Node gates. Never overwrite a published version. Deprecate a defective prerelease or release, publish a corrected version, and provide migration instructions. Generated projects remain pinned until explicitly upgraded.
