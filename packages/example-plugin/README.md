# Generated Hoakbot plugin

Requires Node 22+ and `@hoakbot/plugin-sdk@4.0.0-next.0`. Edit the static manifest, implement lifecycle hooks in `src/index.ts`, then run `npm run typecheck && npm test && npm run build`.
